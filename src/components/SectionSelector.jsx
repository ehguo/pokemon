import React from 'react';

const SectionSelector = (props) => {
  return (
    <div>
      <button>Info</button>
      <button>Moves</button>
      <button>Bingos</button>
    </div>
  )
}

export default SectionSelector;
